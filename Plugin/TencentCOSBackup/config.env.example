# 腾讯云COS配置
#TENCENTCLOUD_SECRET_ID=your_secret_id_here
#TENCENTCLOUD_SECRET_KEY=your_secret_key_here
#以上两项腾讯云密钥请前往腾讯云https://console.cloud.tencent.com/cam/capi获取
#此插件需要腾讯云 COS 存储桶支持，请确保已创建存储桶
#不建议在这里配置TENCENTCLOUD_SECRET_ID与TENCENTCLOUD_SECRET_KEY，建议使用配置子用户权限建立或环境变量更安全的方式管理密钥

# 基础COS配置
#腾讯云 COS 存储桶名称
COS_BUCKET_NAME=test-111111652 
#腾讯云 COS 存储桶所在地域
COS_REGION=ap-hongkong 

# AgentAI操作文件夹的父目录配置
# AgentAI操作文件夹将在此父目录下创建
#AgentAI操作文件夹的父目录名称，可自定义
AGENT_PARENT_DIR=VCPAgentAI 

# AgentAI操作文件夹配置
# 格式：文件夹名:上传权限:列出权限:下载权限:删除权限:复制和移动权限
# 权限值：true=允许，false=禁止
# 多个文件夹用逗号分隔
# 这些文件夹将在AGENT_PARENT_DIR下自动创建
AGENT_FOLDERS_CONFIG=agent-data:true:true:true:true:false,agent-temp:true:true:true:true:true,agent-readonly:false:true:false:false:false

# 配置说明：
# agent-data: 允许上传、列出、下载、复制和移动、禁止删除
# agent-temp: 允许所有操作（上传、列出、下载、复制和移动、删除）
# agent-readonly: 只允许列出，禁止其他操作
#
# 腾讯云 COS 存储桶文件夹结构示例：
# VCPAgentAI/
# ├── agent-data/
# ├── agent-temp/
# └── agent-readonly/

# 异步回调配置
CALLBACK_BASE_URL=http://localhost:6005/plugin-callback

#调试模式:是否启用调试模式，会输出详细日志
DEBUG_MODE=false 

#日志记录配置:是否启用日志记录功能，true=启用，false=禁用
ENABLE_LOGGING=false 

# 文件压缩配置,文件大小超过此值（MB）时自动压缩为zip上传
COMPRESS_THRESHOLD_MB=100 
