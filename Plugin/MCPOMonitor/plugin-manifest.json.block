{
  "manifestVersion": "1.0.0",
  "name": "MCPOMonitor",
  "version": "1.0.0",
  "displayName": "MCPO 服务状态监控器",
  "description": "监控 MCPO 服务器状态并提供所有可用 MCP 工具的详细信息，通过 {{MCPOServiceStatus}} 占位符集成到系统提示词中。",
  "author": "VCP Team",
  "pluginType": "static",
  "entryPoint": {
    "type": "nodejs",
    "command": "node mcpo_monitor.js"
  },
  "communication": {
    "protocol": "stdio",
    "timeout": 60000
  },
  "capabilities": {
    "systemPromptPlaceholders": [
      {
        "placeholder": "{{MCPOServiceStatus}}",
        "description": "MCPO 服务器的详细状态信息与所有可用工具的完整使用说明。"
      }
    ],
    "invocationCommands": []
  },
  "configSchema": {
    "MCPO_HOST": {
      "type": "string",
      "description": "MCPO 服务器完整地址（如果指定则优先使用）",
      "default": "http://0.0.0.0:9000"
    },
    "MCPO_PORT": {
      "type": "integer",
      "description": "MCPO 服务器端口号（与MCPO插件共享配置）",
      "default": 9000
    },
    "MCPO_API_KEY": {
      "type": "string", 
      "description": "MCPO 服务器 API 密钥（与MCPO插件共享配置）",
      "default": "vcp-mcpo-secret"
    },
    "ENABLE_CACHE": {
      "type": "boolean",
      "description": "启用缓存机制",
      "default": true
    },
    "CACHE_TTL_MINUTES": {
      "type": "integer",
      "description": "缓存有效期（分钟）。由于每10秒都会实时检测，缓存主要用作故障时的备用",
      "default": 2
    },
    "INCLUDE_DETAILED_PARAMS": {
      "type": "boolean", 
      "description": "包含详细的参数信息",
      "default": true
    },
    "HEALTH_CHECK_TIMEOUT": {
      "type": "integer",
      "description": "健康检查超时时间（毫秒）",
      "default": 5000
    },
    "REFRESH_INTERVAL_CRON": {
      "type": "string",
      "description": "自动更新间隔（Cron表达式）。默认每10秒更新一次，确保占位符获取实时信息",
      "default": "*/10 * * * * *"
    }
  },
  "refreshIntervalCron": "*/10 * * * * *"
}