# 输出模式: 'server' | 'custom' | 'default'
OUTPUT_MODE=server

# 模式一：服务器目录（OUTPUT_MODE=server 时生效）
SERVER_ROOT=/var/www/html/midi-output

# 模式二：自定义路径（OUTPUT_MODE=custom 时生效）
# 支持绝对路径或相对于插件目录的相对路径
CUSTOM_PATH=/home/user/my-midi-files
# 或相对路径：CUSTOM_PATH=../../shared/midi